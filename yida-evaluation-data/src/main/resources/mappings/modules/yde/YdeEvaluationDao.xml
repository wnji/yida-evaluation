<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yida.modules.yde.dao.YdeEvaluationDao">
	
	<!-- 查询数据
	<select id="findList" resultType="YdeEvaluation">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<select id="findTotalScore" resultType="int">
		select sum(points) from yde_question where status=0 and evaluation_id=#{evaluationId}
	</select>
</mapper>